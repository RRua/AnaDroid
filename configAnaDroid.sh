#!/bin/bash
source ../settings/settings.sh


# download sdkman (or manually install sdk)
# set android home
# install gradle? (via sdkman or brew)
# install coreutils (brew install coreutils findutils gnu-tar gnu-sed gawk gnutls gnu-indent gnu-getopt grep)
